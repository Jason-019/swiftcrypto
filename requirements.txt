# SwiftCryptoV3 依赖
# 安装: pip install -r requirements.txt

# GUI 框架
PyQt6>=6.5

# 加密核心
pycryptodome>=3.19    # AES-GCM, SHA256
base91>=1.0           # Base91 编码（ASCII 安全）

# 密钥派生 (使用 hashlib 标准库，无需额外依赖)

# 数据处理
numpy>=1.24

# 时区支持
pytz>=2024.1

# 注意：bcrypt 已不再需要（已替换为 PBKDF2-HMAC-SHA256）
# 注意：rapidfuzz 仅被 Encrypt_Text_Edit.py 使用（该文件为死代码，已归档）
